package com.samra;
//conditional statement
//either same first element OR same last element
//Both arrays will be length 1 OR more



public class commonEnd {

    public boolean commonEnd(int[] a, int[] b) {
        int num1 = 0;
        int num2 = 0;

        if (a[0] == b[0]) || a[a.length - 1] == b[b.length - 1]{

        }
        return true;

        else

        return false;
    }
}
